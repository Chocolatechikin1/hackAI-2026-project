
  # Degree planner app wireframe

  This is a code bundle for Degree planner app wireframe. The original project is available at https://www.figma.com/design/jx2RqtbyF38MiDD6vSuTrr/Degree-planner-app-wireframe.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  